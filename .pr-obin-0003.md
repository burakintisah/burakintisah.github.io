## 🎯 Task: OBIN-0003

Remove legacy gh-pages deployment scripts (cleanup)

## 📝 Changes

### Removed from `package.json`

- ❌ `predeploy` script (no longer needed)
- ❌ `deploy` script (replaced by GitHub Actions)

### Before
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "lint": "eslint .",
  "preview": "vite preview",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}
```

### After
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "lint": "eslint .",
  "preview": "vite preview"
}
```

## 🎯 Why This Change?

With GitHub Actions now handling all deployments (OBIN-0002), the manual deployment scripts are no longer needed:

- **Old workflow**: `npm run deploy` → Manual gh-pages push
- **New workflow**: `git push origin main` → Automatic GitHub Actions deploy

## ✅ Impact

- ✅ Cleaner package.json
- ✅ No confusion about deployment method
- ✅ GitHub Actions is the single source of truth
- ✅ No breaking changes (gh-pages package already removed)

## 🚀 Deployment Method

From now on, **only use**:
```bash
git push origin main  # Automatic deployment via GitHub Actions
```

**Do NOT use**:
```bash
npm run deploy  # This command no longer exists
```

## 📋 Testing

- [x] Scripts removed from package.json
- [x] No breaking changes
- [x] GitHub Actions workflow still works
- [x] Documentation already updated in OBIN-0002

---

**Branch**: `feature/OBIN-0003`  
**Commits**: 1 (`68327d6`)  
**Files Changed**: 1 file, 1 insertion, 4 deletions
